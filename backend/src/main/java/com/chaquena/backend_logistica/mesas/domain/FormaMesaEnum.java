package com.chaquena.backend_logistica.mesas.domain;

/**
 * Como se dibuja la mesa en el plano. Cuadrada cubre tambien las
 * rectangulares: el ancho y el alto dicen el tamano; la forma, si tiene
 * esquinas.
 */
public enum FormaMesaEnum {
    CUADRADA,
    REDONDA
}
